<h1>Pantalon</h1>

<p>
    @foreach(var pants in Model.Pants)
    {
    <li>
        @pants.Image<br>
        @pants.Name<br>
        @pants.Price
    </li>

    }
</p>

<h1>PLayera</h1>

<p>
    @foreach (var playeras in Model.Shirts)
    {
    <p>
            @Html.DisplayFor(modelItem => playeras.Size)
    </p>
    <li>
            @Html.DisplayFor(modelItem => playeras.Price)
    </li>
    }
</p>